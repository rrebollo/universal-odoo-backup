#!/usr/bin/env bash
# odoo-backup-remote.sh — Remote Odoo backup via SSH + rsync
set -euo pipefail

# ─── Defaults ────────────────────────────────────────────────────────────────
REMOTE_CONF="${REMOTE_CONF:-/etc/odoo/odoo.conf}"
BACKUP_FORMAT="${BACKUP_FORMAT:-zip}"
LOCAL_DIR="$(pwd)"
REMOTE_BACKUP_DIR="${REMOTE_BACKUP_DIR:-/opt/odoo-server/backups}"
DRY_RUN=false
VERBOSE=false
SSH_USER_HOST=""
DB_NAME=""

# ─── usage ───────────────────────────────────────────────────────────────────
usage() {
  cat <<EOF
Usage: $(basename "$0") [OPTIONS] <user@host> [<db_name>] [<local_backup_dir>]

Backup an Odoo database from a remote host via SSH + rsync.

Arguments:
  <user@host>         Remote host to connect to
  [<db_name>]         Database name (default: extracted from hostname)
  [<local_backup_dir>]  Local directory for backup (default: current directory)

Options:
  -h, --help          Show this help and exit
  -c, --conf PATH     Remote odoo.conf path (default: /etc/odoo/odoo.conf)
  -r, --remote-dir    Remote backup directory (default: /opt/odoo-server/backups)
  -f, --format FMT    Backup format: zip|dump (default: zip)
  -d, --dry-run       Show what would happen without executing
  -v, --verbose       Show debug output from SSH and rsync

Examples:
  $(basename "$0") admin@prod1.binex.cloud
  $(basename "$0") admin@prod1.binex.cloud ./backups/
  $(basename "$0") admin@prod1.binex.cloud mydb ./backups/
  $(basename "$0") -f dump admin@host mydb
  $(basename "$0") -d admin@host mydb  # dry run
EOF
}

# ─── parse_args ──────────────────────────────────────────────────────────────
parse_args() {
  while [[ $# -gt 0 ]]; do
    case "$1" in
      -h|--help)
        usage
        exit 0
        ;;
      -c|--conf)
        REMOTE_CONF="$2"
        shift 2
        ;;
      -r|--remote-dir)
        REMOTE_BACKUP_DIR="$2"
        shift 2
        ;;
      -f|--format)
        BACKUP_FORMAT="$2"
        shift 2
        ;;
      -d|--dry-run)
        DRY_RUN=true
        shift
        ;;
      -v|--verbose)
        VERBOSE=true
        shift
        ;;
      -*)
        echo "[ERROR] Unknown option: $1" >&2
        echo "Run '$(basename "$0") --help' for usage." >&2
        exit 1
        ;;
      *)
        if [ -z "$SSH_USER_HOST" ]; then
          SSH_USER_HOST="$1"
          shift
        elif [ -z "$DB_NAME" ]; then
          # If second arg contains '/', it's a local directory path, not db_name
          if [[ "$1" == */* ]]; then
            LOCAL_DIR="$1"
          else
            DB_NAME="$1"
          fi
          shift
        elif [ "$LOCAL_DIR" = "$(pwd)" ]; then
          LOCAL_DIR="$1"
          shift
        else
          echo "[ERROR] Unexpected argument: $1" >&2
          echo "Run '$(basename "$0") --help' for usage." >&2
          exit 1
        fi
        ;;
    esac
  done
}

# ─── extract_db_from_hostname ────────────────────────────────────────────────
extract_db_from_hostname() {
  local host_part="${SSH_USER_HOST#*@}"

  if [[ "$host_part" =~ ^([a-zA-Z0-9_-]+)\.binhex\.cloud$ ]]; then
    DB_NAME="${BASH_REMATCH[1]}"
  else
    DB_NAME="$host_part"
  fi
}

# ─── resolve_local_script ─────────────────────────────────────────────────────
resolve_local_script() {
  if command -v odoo-backup-local &>/dev/null; then
    command -v odoo-backup-local
  elif [ -f "$(dirname "$0")/odoo-backup-local" ]; then
    echo "$(dirname "$0")/odoo-backup-local"
  else
    echo "[ERROR] odoo-backup-local not found in PATH or alongside this script." >&2
    exit 1
  fi
}

# ─── ssh_cmd ─────────────────────────────────────────────────────────────────
ssh_cmd() {
  if $VERBOSE; then
    ssh "$SSH_USER_HOST" "$@"
  else
    ssh -q "$SSH_USER_HOST" "$@"
  fi
}

# ─── get_remote_filestore_size ───────────────────────────────────────────────
get_remote_filestore_size() {
  local data_dir
  data_dir=$(ssh_cmd "grep -E '^\s*data_dir\s*=' $REMOTE_CONF 2>/dev/null | awk -F'=' '{print \$2}' | sed 's/^[[:space:]]*//;s/[[:space:]]*\$//'" || true)

  if [ -z "$data_dir" ]; then
    # Use getent to get odoo user's home directory reliably
    data_dir=$(ssh_cmd "getent passwd odoo | cut -d: -f6")/.local/share/Odoo
  fi

  local fs_path="${data_dir}/filestore/${DB_NAME}"

  local size_mb
  size_mb=$(ssh_cmd "sudo -u odoo du -sm '$fs_path' 2>/dev/null | awk '{print \$1}'" 2>/dev/null || true)
  size_mb="${size_mb:-0}"

  echo "$size_mb"
}

# ─── check_remote_space ──────────────────────────────────────────────────────
check_remote_space() {
  local fs_size_mb="$1"

  local estimated_mb
  if [ "$fs_size_mb" -gt 0 ] 2>/dev/null; then
    estimated_mb=$(( fs_size_mb * 7 / 10 ))
  else
    estimated_mb=5120
  fi

  local available_kb
  available_kb=$(ssh_cmd "df -k / | awk 'NR==2 {print \$4}'" 2>/dev/null || echo "0")
  available_kb="${available_kb:-0}"
  if ! [[ "$available_kb" =~ ^[0-9]+$ ]]; then
    available_kb=0
  fi
  local available_gb=$(( available_kb / 1024 / 1024 ))
  local available_mb=$(( available_kb / 1024 ))

  local threshold_mb=$(( estimated_mb * 3 / 2 ))

  echo "[INFO] Estimated backup size: $(awk "BEGIN {printf \"%.1f\", $estimated_mb/1024}") GB (${estimated_mb} MB)"
  echo "[INFO] Available space: ${available_gb} GB"

  if [ "$available_mb" -lt "$threshold_mb" ]; then
    echo "[WARN] Low disk space. Estimated: $(( estimated_mb / 1024 )) GB, Available: ${available_gb} GB"
    read -rp "Continue? [y/N] " response
    if [[ "$response" != [yY]* ]]; then
      echo "[INFO] Aborted by user."
      exit 0
    fi
  fi
}

# ─── run_remote_backup ───────────────────────────────────────────────────────
run_remote_backup() {
  echo "[INFO] Starting remote backup..." >&2
  echo "[INFO] Host     : $SSH_USER_HOST" >&2
  echo "[INFO] Database : $DB_NAME" >&2
  echo "[INFO] Format   : $BACKUP_FORMAT" >&2
  echo "[INFO] Config   : $REMOTE_CONF" >&2
  echo "" >&2

  local output
  local script_file="/tmp/odoo-backup-$$.sh"
  
  # Copy script to remote temp file and execute it to avoid stdin complications
  # This prevents issues with interactive prompts or script reading stdin
  local local_script
  local_script=$(resolve_local_script)
  scp -q "$local_script" "$SSH_USER_HOST:$script_file" || {
    echo "[ERROR] Failed to copy backup script to remote host." >&2
    return 1
  }
  
  # Ensure remote backup directory exists and is writable by odoo user
  ssh_cmd "sudo mkdir -p '$REMOTE_BACKUP_DIR' && sudo chown odoo:odoo '$REMOTE_BACKUP_DIR'" || {
    echo "[ERROR] Failed to prepare remote backup directory: $REMOTE_BACKUP_DIR" >&2
    return 1
  }

   local output_file
   output_file=$(mktemp)
   ssh_cmd "sudo -u odoo env ODOO_CONF='$REMOTE_CONF' BACKUP_FORMAT='$BACKUP_FORMAT' BACKUP_DIR='$REMOTE_BACKUP_DIR' bash $script_file $DB_NAME" 2>&1 | tee "$output_file" >&2

  # Clean up remote temp file
  ssh_cmd "rm -f $script_file" 2>/dev/null || true

  local output
  output=$(cat "$output_file")
  rm -f "$output_file"

   local backup_file
   backup_file=$(echo "$output" | grep '\[INFO\] Done:' | tail -1 | sed 's/.*Done: \([^ ]*\).*/\1/')

   if [ -z "$backup_file" ]; then
     echo "[ERROR] Could not determine remote backup file path." >&2
     echo "[ERROR] Remote output was:" >&2
     echo "$output" >&2
     exit 1
   fi

   # Make backup file readable by rsync user before returning
   ssh_cmd "sudo chmod o+r '$backup_file'" 2>/dev/null || {
     echo "[WARN] Could not chmod backup file, rsync may fail due to permissions." >&2
   }

   # Return backup file path via stdout ONLY - all logging goes to stderr
   echo "$backup_file"
}

# ─── check_partial_download ───────────────────────────────────────────────────
check_partial_download() {
  mkdir -p "$LOCAL_DIR"

  local partial
  partial=$(find "$LOCAL_DIR" -maxdepth 1 -name "${DB_NAME}_*" \( -name "*.dump" -o -name "*.zip" \) -size +0 2>/dev/null | sort | tail -1)

  if [ -z "$partial" ]; then
    return 1
  fi

  local partial_size
  partial_size=$(du -h "$partial" | awk '{print $1}')
  echo "[INFO] Found existing local file: $(basename "$partial") ($partial_size)" >&2

  local remote_path="$REMOTE_BACKUP_DIR/$(basename "$partial")"
  local remote_size
  remote_size=$(ssh_cmd "sudo -u odoo stat -c%s '$remote_path' 2>/dev/null" || echo "0")
  local local_bytes
  local_bytes=$(stat -c%s "$partial" 2>/dev/null || echo "0")

  if [ "$remote_size" != "0" ] && [ "$local_bytes" = "$remote_size" ]; then
    echo "[INFO] Local file matches remote size — backup already complete." >&2
    echo "[INFO] Backup exists at: $LOCAL_DIR/$(basename "$partial")" >&2
    exit 0
  fi

  read -rp "Resume download of existing file? [Y/n] " response
  if [[ "$response" == [nN]* ]]; then
    rm -f "$partial"
    echo "[INFO] Removed existing file, will create fresh." >&2
    return 1
  fi
  echo "$partial"
}

# ─── list_remote_backups ──────────────────────────────────────────────────────
list_remote_backups() {
  local ext="$BACKUP_FORMAT"
  ssh_cmd "sudo -u odoo find '$REMOTE_BACKUP_DIR' -maxdepth 1 -name '${DB_NAME}*.$ext' -exec ls -lhS {} + 2>/dev/null" || true
}

# ─── check_remote_backups ─────────────────────────────────────────────────────
check_remote_backups() {
  local remote_files
  remote_files=$(list_remote_backups)

  if [ -z "$remote_files" ]; then
    return 1
  fi

  echo "[INFO] Found existing remote backups for '$DB_NAME':" >&2
  echo "$remote_files" | while IFS= read -r line; do
    echo "       $line" >&2
  done

  local latest
  latest=$(echo "$remote_files" | head -1 | awk '{print $NF}')

  read -rp "Download existing backup instead of creating a new one? [Y/n] " response
  if [[ "$response" == [nN]* ]]; then
    return 1
  fi
  echo "$latest"
}

# ─── offer_remote_cleanup ─────────────────────────────────────────────────────
offer_remote_cleanup() {
  local remote_dir="$1"

  local stale_output
  stale_output=$(ssh_cmd "sudo -u odoo find '$remote_dir' -maxdepth 1 -name '${DB_NAME}*.$BACKUP_FORMAT' -exec ls -lh {} + 2>/dev/null" || true)

  if [ -z "$stale_output" ]; then
    return 0
  fi

  echo "[INFO] Stale remote backups found in $remote_dir:" >&2
  echo "$stale_output" | while IFS= read -r line; do
    echo "       $line" >&2
  done

  read -rp "Clean them up? [y/N] " response
  if [[ "$response" == [yY]* ]]; then
    ssh_cmd "sudo -u odoo find '$remote_dir' -maxdepth 1 -name '${DB_NAME}*.$BACKUP_FORMAT' -exec rm -f {} + 2>/dev/null" && \
      echo "[INFO] Remote stale backups cleaned." >&2 || \
      echo "[WARN] Could not clean remote backups." >&2
  fi
}

# ─── rsync_backup ─────────────────────────────────────────────────────────────
rsync_backup() {
  local remote_file="$1"
  local resume_file="$2"

  echo "[INFO] Pulling backup via rsync..."
  mkdir -p "$LOCAL_DIR"

  local rsync_flags="-a --progress --partial"
  local target="$LOCAL_DIR/"
  if [ -n "$resume_file" ]; then
    rsync_flags="$rsync_flags --append-verify"
    target="$resume_file"
    echo "[INFO] Resuming transfer (--append-verify)..."
  fi

  if $VERBOSE; then
    rsync ${rsync_flags} -v "$SSH_USER_HOST:$remote_file" "$target"
  else
    rsync ${rsync_flags} "$SSH_USER_HOST:$remote_file" "$target"
  fi

  local exit_code=$?
  if [ $exit_code -ne 0 ]; then
    echo "[ERROR] rsync failed with exit code $exit_code" >&2
    echo "[INFO] Remote backup preserved at: $remote_file" >&2
    exit 1
  fi

  echo "[INFO] Backup pulled to: $LOCAL_DIR/"
}

# ─── cleanup_remote ──────────────────────────────────────────────────────────
cleanup_remote() {
  local remote_file="$1"

  echo "[INFO] Cleaning up remote backup..."
  if ssh_cmd "sudo -u odoo rm -f $remote_file" 2>/dev/null; then
    echo "[INFO] Remote cleanup complete."
  else
    echo "[WARN] Failed to clean up remote file: $remote_file" >&2
  fi
}

# ─── main ────────────────────────────────────────────────────────────────────
main() {
  parse_args "$@"

  if [ -z "${SSH_USER_HOST:-}" ]; then
    echo "[ERROR] Remote host required. Run '$(basename "$0") --help' for usage." >&2
    exit 1
  fi

  if [ -z "$DB_NAME" ]; then
    extract_db_from_hostname
    echo "[INFO] Using database name from hostname: $DB_NAME"
  fi

  if $DRY_RUN; then
    echo "[DRY RUN] Would connect to: $SSH_USER_HOST"
    echo "[DRY RUN] Database: $DB_NAME"
    echo "[DRY RUN] Config: $REMOTE_CONF"
    echo "[DRY RUN] Format: $BACKUP_FORMAT"
    echo "[DRY RUN] Local dir: $LOCAL_DIR"
    exit 0
  fi

  if [ "$BACKUP_FORMAT" != "dump" ]; then
    echo "[INFO] Estimating backup size..."
    local fs_size_mb
    fs_size_mb=$(get_remote_filestore_size)
    if [ "$fs_size_mb" -gt 0 ] 2>/dev/null; then
      echo "[INFO] Filestore size: $(awk "BEGIN {printf \"%.1f\", $fs_size_mb/1024}") GB (${fs_size_mb} MB)"
    else
      echo "[WARN] Could not determine filestore size, using default estimate."
    fi

    check_remote_space "$fs_size_mb"
  fi

  local partial_file
  partial_file=$(check_partial_download || true)

  if [ -n "$partial_file" ]; then
    local backup_file="$REMOTE_BACKUP_DIR/$(basename "$partial_file")"
    rsync_backup "$backup_file" "$partial_file"
    cleanup_remote "$backup_file"
  else
    local remote_file
    remote_file=$(check_remote_backups || true)

    if [ -n "$remote_file" ]; then
      echo "[INFO] Downloading existing remote backup..." >&2
      rsync_backup "$remote_file" ""
      cleanup_remote "$remote_file"
    else
      local backup_file
      backup_file=$(run_remote_backup)
      rsync_backup "$backup_file" ""
      cleanup_remote "$backup_file"
    fi
  fi

  offer_remote_cleanup "$REMOTE_BACKUP_DIR"

  echo "" >&2
  echo "[INFO] All done. Backup saved to: $LOCAL_DIR/" >&2
}

main "$@"